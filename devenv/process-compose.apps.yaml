version: "0.5"
is_strict: true
ordered_shutdown: true

env_cmds:
  BACKEND_PORT: bun --no-env-file --install=fallback devenv/scripts/get-port.ts backend
  APP_PORT: bun --no-env-file --install=fallback devenv/scripts/get-port.ts frontend
  DOCUMENTS_PORT: bun --no-env-file --install=fallback devenv/scripts/get-port.ts documents

processes:
  localias-setup:
    description: Register workspace service aliases
    command: |
      set -eu
      localias set "$${API_URL#https://}" "$${BACKEND_PORT}"
      localias set "$${APP_URL#https://}" "$${APP_PORT}"
      localias set "$${DOCUMENTS_URL#https://}" "$${DOCUMENTS_PORT}"
    availability:
      restart: "no"

  backend:
    availability:
      restart: on_failure
      max_restarts: 2
    description: Run the backend development server
    command: just backend::dev
    readiness_probe:
      exec:
        command: wget -q -O /dev/null "http://localhost:$${BACKEND_PORT}/ready"
      period_seconds: 1
      timeout_seconds: 2
      failure_threshold: 30
    depends_on:
      localias-setup:
        condition: process_completed_successfully

  frontend:
    availability:
      restart: on_failure
      max_restarts: 2
    description: Run the frontend development server
    command: just frontend::dev
    depends_on:
      backend:
        condition: process_healthy
      localias-setup:
        condition: process_completed_successfully
    readiness_probe:
      exec:
        command: wget -q -O /dev/null "http://localhost:$${APP_PORT}/"
      period_seconds: 1
      timeout_seconds: 2
      failure_threshold: 30

  documents:
    availability:
      restart: on_failure
      max_restarts: 2
    description: Run the collaborative documents development server
    command: just documents-server::dev
    readiness_probe:
      exec:
        command: wget -q -O /dev/null "http://localhost:$${DOCUMENTS_PORT}/health"
      period_seconds: 1
      timeout_seconds: 2
      failure_threshold: 30
    depends_on:
      localias-setup:
        condition: process_completed_successfully
