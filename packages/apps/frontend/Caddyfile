{
	auto_https off
}

:80 {
    handle_path /api/* {
        reverse_proxy {env.PROXY_API_UPSTREAM_HOST}
    }
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
