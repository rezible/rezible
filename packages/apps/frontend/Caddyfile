{
	auto_https off
}

{env.APP_HOST}:80 {
    handle_path /api* {
        reverse_proxy {env.PROXY_BACKEND_UPSTREAM_HOST}
    }

    handle /dex* {
        reverse_proxy {env.PROXY_DEX_UPSTREAM_HOST}
    }

	root * /srv
	try_files {path} /index.html
	file_server
}
