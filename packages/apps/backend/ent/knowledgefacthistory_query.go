// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/rezible/rezible/ent/internal"
	"github.com/rezible/rezible/ent/knowledgeentityalias"
	"github.com/rezible/rezible/ent/knowledgefacthistory"
	"github.com/rezible/rezible/ent/knowledgerelationship"
	"github.com/rezible/rezible/ent/normalizedevent"
	"github.com/rezible/rezible/ent/predicate"
	"github.com/rezible/rezible/ent/tenant"
)

// KnowledgeFactHistoryQuery is the builder for querying KnowledgeFactHistory entities.
type KnowledgeFactHistoryQuery struct {
	config
	ctx                 *QueryContext
	order               []knowledgefacthistory.OrderOption
	inters              []Interceptor
	predicates          []predicate.KnowledgeFactHistory
	withTenant          *TenantQuery
	withAlias           *KnowledgeEntityAliasQuery
	withRelationship    *KnowledgeRelationshipQuery
	withNormalizedEvent *NormalizedEventQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KnowledgeFactHistoryQuery builder.
func (_q *KnowledgeFactHistoryQuery) Where(ps ...predicate.KnowledgeFactHistory) *KnowledgeFactHistoryQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *KnowledgeFactHistoryQuery) Limit(limit int) *KnowledgeFactHistoryQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *KnowledgeFactHistoryQuery) Offset(offset int) *KnowledgeFactHistoryQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *KnowledgeFactHistoryQuery) Unique(unique bool) *KnowledgeFactHistoryQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *KnowledgeFactHistoryQuery) Order(o ...knowledgefacthistory.OrderOption) *KnowledgeFactHistoryQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTenant chains the current query on the "tenant" edge.
func (_q *KnowledgeFactHistoryQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgefacthistory.Table, knowledgefacthistory.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, knowledgefacthistory.TenantTable, knowledgefacthistory.TenantColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.Tenant
		step.Edge.Schema = schemaConfig.KnowledgeFactHistory
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAlias chains the current query on the "alias" edge.
func (_q *KnowledgeFactHistoryQuery) QueryAlias() *KnowledgeEntityAliasQuery {
	query := (&KnowledgeEntityAliasClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgefacthistory.Table, knowledgefacthistory.FieldID, selector),
			sqlgraph.To(knowledgeentityalias.Table, knowledgeentityalias.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, knowledgefacthistory.AliasTable, knowledgefacthistory.AliasColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.KnowledgeEntityAlias
		step.Edge.Schema = schemaConfig.KnowledgeFactHistory
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRelationship chains the current query on the "relationship" edge.
func (_q *KnowledgeFactHistoryQuery) QueryRelationship() *KnowledgeRelationshipQuery {
	query := (&KnowledgeRelationshipClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgefacthistory.Table, knowledgefacthistory.FieldID, selector),
			sqlgraph.To(knowledgerelationship.Table, knowledgerelationship.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, knowledgefacthistory.RelationshipTable, knowledgefacthistory.RelationshipColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.KnowledgeRelationship
		step.Edge.Schema = schemaConfig.KnowledgeFactHistory
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNormalizedEvent chains the current query on the "normalized_event" edge.
func (_q *KnowledgeFactHistoryQuery) QueryNormalizedEvent() *NormalizedEventQuery {
	query := (&NormalizedEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgefacthistory.Table, knowledgefacthistory.FieldID, selector),
			sqlgraph.To(normalizedevent.Table, normalizedevent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, knowledgefacthistory.NormalizedEventTable, knowledgefacthistory.NormalizedEventColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.NormalizedEvent
		step.Edge.Schema = schemaConfig.KnowledgeFactHistory
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first KnowledgeFactHistory entity from the query.
// Returns a *NotFoundError when no KnowledgeFactHistory was found.
func (_q *KnowledgeFactHistoryQuery) First(ctx context.Context) (*KnowledgeFactHistory, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{knowledgefacthistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) FirstX(ctx context.Context) *KnowledgeFactHistory {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KnowledgeFactHistory ID from the query.
// Returns a *NotFoundError when no KnowledgeFactHistory ID was found.
func (_q *KnowledgeFactHistoryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{knowledgefacthistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KnowledgeFactHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KnowledgeFactHistory entity is found.
// Returns a *NotFoundError when no KnowledgeFactHistory entities are found.
func (_q *KnowledgeFactHistoryQuery) Only(ctx context.Context) (*KnowledgeFactHistory, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{knowledgefacthistory.Label}
	default:
		return nil, &NotSingularError{knowledgefacthistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) OnlyX(ctx context.Context) *KnowledgeFactHistory {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KnowledgeFactHistory ID in the query.
// Returns a *NotSingularError when more than one KnowledgeFactHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *KnowledgeFactHistoryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{knowledgefacthistory.Label}
	default:
		err = &NotSingularError{knowledgefacthistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KnowledgeFactHistories.
func (_q *KnowledgeFactHistoryQuery) All(ctx context.Context) ([]*KnowledgeFactHistory, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*KnowledgeFactHistory, *KnowledgeFactHistoryQuery]()
	return withInterceptors[[]*KnowledgeFactHistory](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) AllX(ctx context.Context) []*KnowledgeFactHistory {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KnowledgeFactHistory IDs.
func (_q *KnowledgeFactHistoryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(knowledgefacthistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *KnowledgeFactHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*KnowledgeFactHistoryQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *KnowledgeFactHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *KnowledgeFactHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KnowledgeFactHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *KnowledgeFactHistoryQuery) Clone() *KnowledgeFactHistoryQuery {
	if _q == nil {
		return nil
	}
	return &KnowledgeFactHistoryQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]knowledgefacthistory.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.KnowledgeFactHistory{}, _q.predicates...),
		withTenant:          _q.withTenant.Clone(),
		withAlias:           _q.withAlias.Clone(),
		withRelationship:    _q.withRelationship.Clone(),
		withNormalizedEvent: _q.withNormalizedEvent.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeFactHistoryQuery) WithTenant(opts ...func(*TenantQuery)) *KnowledgeFactHistoryQuery {
	query := (&TenantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTenant = query
	return _q
}

// WithAlias tells the query-builder to eager-load the nodes that are connected to
// the "alias" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeFactHistoryQuery) WithAlias(opts ...func(*KnowledgeEntityAliasQuery)) *KnowledgeFactHistoryQuery {
	query := (&KnowledgeEntityAliasClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAlias = query
	return _q
}

// WithRelationship tells the query-builder to eager-load the nodes that are connected to
// the "relationship" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeFactHistoryQuery) WithRelationship(opts ...func(*KnowledgeRelationshipQuery)) *KnowledgeFactHistoryQuery {
	query := (&KnowledgeRelationshipClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRelationship = query
	return _q
}

// WithNormalizedEvent tells the query-builder to eager-load the nodes that are connected to
// the "normalized_event" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeFactHistoryQuery) WithNormalizedEvent(opts ...func(*NormalizedEventQuery)) *KnowledgeFactHistoryQuery {
	query := (&NormalizedEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNormalizedEvent = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID int `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.KnowledgeFactHistory.Query().
//		GroupBy(knowledgefacthistory.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *KnowledgeFactHistoryQuery) GroupBy(field string, fields ...string) *KnowledgeFactHistoryGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &KnowledgeFactHistoryGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = knowledgefacthistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID int `json:"tenant_id,omitempty"`
//	}
//
//	client.KnowledgeFactHistory.Query().
//		Select(knowledgefacthistory.FieldTenantID).
//		Scan(ctx, &v)
func (_q *KnowledgeFactHistoryQuery) Select(fields ...string) *KnowledgeFactHistorySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &KnowledgeFactHistorySelect{KnowledgeFactHistoryQuery: _q}
	sbuild.label = knowledgefacthistory.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a KnowledgeFactHistorySelect configured with the given aggregations.
func (_q *KnowledgeFactHistoryQuery) Aggregate(fns ...AggregateFunc) *KnowledgeFactHistorySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *KnowledgeFactHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !knowledgefacthistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	if knowledgefacthistory.Policy == nil {
		return errors.New("ent: uninitialized knowledgefacthistory.Policy (forgotten import ent/runtime?)")
	}
	if err := knowledgefacthistory.Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *KnowledgeFactHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KnowledgeFactHistory, error) {
	var (
		nodes       = []*KnowledgeFactHistory{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withTenant != nil,
			_q.withAlias != nil,
			_q.withRelationship != nil,
			_q.withNormalizedEvent != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KnowledgeFactHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KnowledgeFactHistory{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = _q.schemaConfig.KnowledgeFactHistory
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTenant; query != nil {
		if err := _q.loadTenant(ctx, query, nodes, nil,
			func(n *KnowledgeFactHistory, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAlias; query != nil {
		if err := _q.loadAlias(ctx, query, nodes, nil,
			func(n *KnowledgeFactHistory, e *KnowledgeEntityAlias) { n.Edges.Alias = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRelationship; query != nil {
		if err := _q.loadRelationship(ctx, query, nodes, nil,
			func(n *KnowledgeFactHistory, e *KnowledgeRelationship) { n.Edges.Relationship = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNormalizedEvent; query != nil {
		if err := _q.loadNormalizedEvent(ctx, query, nodes, nil,
			func(n *KnowledgeFactHistory, e *NormalizedEvent) { n.Edges.NormalizedEvent = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *KnowledgeFactHistoryQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*KnowledgeFactHistory, init func(*KnowledgeFactHistory), assign func(*KnowledgeFactHistory, *Tenant)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*KnowledgeFactHistory)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *KnowledgeFactHistoryQuery) loadAlias(ctx context.Context, query *KnowledgeEntityAliasQuery, nodes []*KnowledgeFactHistory, init func(*KnowledgeFactHistory), assign func(*KnowledgeFactHistory, *KnowledgeEntityAlias)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*KnowledgeFactHistory)
	for i := range nodes {
		if nodes[i].AliasID == nil {
			continue
		}
		fk := *nodes[i].AliasID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(knowledgeentityalias.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "alias_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *KnowledgeFactHistoryQuery) loadRelationship(ctx context.Context, query *KnowledgeRelationshipQuery, nodes []*KnowledgeFactHistory, init func(*KnowledgeFactHistory), assign func(*KnowledgeFactHistory, *KnowledgeRelationship)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*KnowledgeFactHistory)
	for i := range nodes {
		if nodes[i].RelationshipID == nil {
			continue
		}
		fk := *nodes[i].RelationshipID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(knowledgerelationship.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "relationship_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *KnowledgeFactHistoryQuery) loadNormalizedEvent(ctx context.Context, query *NormalizedEventQuery, nodes []*KnowledgeFactHistory, init func(*KnowledgeFactHistory), assign func(*KnowledgeFactHistory, *NormalizedEvent)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*KnowledgeFactHistory)
	for i := range nodes {
		if nodes[i].NormalizedEventID == nil {
			continue
		}
		fk := *nodes[i].NormalizedEventID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(normalizedevent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "normalized_event_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *KnowledgeFactHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Schema = _q.schemaConfig.KnowledgeFactHistory
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *KnowledgeFactHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(knowledgefacthistory.Table, knowledgefacthistory.Columns, sqlgraph.NewFieldSpec(knowledgefacthistory.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, knowledgefacthistory.FieldID)
		for i := range fields {
			if fields[i] != knowledgefacthistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTenant != nil {
			_spec.Node.AddColumnOnce(knowledgefacthistory.FieldTenantID)
		}
		if _q.withAlias != nil {
			_spec.Node.AddColumnOnce(knowledgefacthistory.FieldAliasID)
		}
		if _q.withRelationship != nil {
			_spec.Node.AddColumnOnce(knowledgefacthistory.FieldRelationshipID)
		}
		if _q.withNormalizedEvent != nil {
			_spec.Node.AddColumnOnce(knowledgefacthistory.FieldNormalizedEventID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *KnowledgeFactHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(knowledgefacthistory.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = knowledgefacthistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(_q.schemaConfig.KnowledgeFactHistory)
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *KnowledgeFactHistoryQuery) Modify(modifiers ...func(s *sql.Selector)) *KnowledgeFactHistorySelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// KnowledgeFactHistoryGroupBy is the group-by builder for KnowledgeFactHistory entities.
type KnowledgeFactHistoryGroupBy struct {
	selector
	build *KnowledgeFactHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *KnowledgeFactHistoryGroupBy) Aggregate(fns ...AggregateFunc) *KnowledgeFactHistoryGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *KnowledgeFactHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KnowledgeFactHistoryQuery, *KnowledgeFactHistoryGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *KnowledgeFactHistoryGroupBy) sqlScan(ctx context.Context, root *KnowledgeFactHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// KnowledgeFactHistorySelect is the builder for selecting fields of KnowledgeFactHistory entities.
type KnowledgeFactHistorySelect struct {
	*KnowledgeFactHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *KnowledgeFactHistorySelect) Aggregate(fns ...AggregateFunc) *KnowledgeFactHistorySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *KnowledgeFactHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KnowledgeFactHistoryQuery, *KnowledgeFactHistorySelect](ctx, _s.KnowledgeFactHistoryQuery, _s, _s.inters, v)
}

func (_s *KnowledgeFactHistorySelect) sqlScan(ctx context.Context, root *KnowledgeFactHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *KnowledgeFactHistorySelect) Modify(modifiers ...func(s *sql.Selector)) *KnowledgeFactHistorySelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
