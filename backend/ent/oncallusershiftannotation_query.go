// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/twohundreds/rezible/ent/oncallusershift"
	"github.com/twohundreds/rezible/ent/oncallusershiftannotation"
	"github.com/twohundreds/rezible/ent/predicate"
)

// OncallUserShiftAnnotationQuery is the builder for querying OncallUserShiftAnnotation entities.
type OncallUserShiftAnnotationQuery struct {
	config
	ctx        *QueryContext
	order      []oncallusershiftannotation.OrderOption
	inters     []Interceptor
	predicates []predicate.OncallUserShiftAnnotation
	withShift  *OncallUserShiftQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OncallUserShiftAnnotationQuery builder.
func (ousaq *OncallUserShiftAnnotationQuery) Where(ps ...predicate.OncallUserShiftAnnotation) *OncallUserShiftAnnotationQuery {
	ousaq.predicates = append(ousaq.predicates, ps...)
	return ousaq
}

// Limit the number of records to be returned by this query.
func (ousaq *OncallUserShiftAnnotationQuery) Limit(limit int) *OncallUserShiftAnnotationQuery {
	ousaq.ctx.Limit = &limit
	return ousaq
}

// Offset to start from.
func (ousaq *OncallUserShiftAnnotationQuery) Offset(offset int) *OncallUserShiftAnnotationQuery {
	ousaq.ctx.Offset = &offset
	return ousaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ousaq *OncallUserShiftAnnotationQuery) Unique(unique bool) *OncallUserShiftAnnotationQuery {
	ousaq.ctx.Unique = &unique
	return ousaq
}

// Order specifies how the records should be ordered.
func (ousaq *OncallUserShiftAnnotationQuery) Order(o ...oncallusershiftannotation.OrderOption) *OncallUserShiftAnnotationQuery {
	ousaq.order = append(ousaq.order, o...)
	return ousaq
}

// QueryShift chains the current query on the "shift" edge.
func (ousaq *OncallUserShiftAnnotationQuery) QueryShift() *OncallUserShiftQuery {
	query := (&OncallUserShiftClient{config: ousaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ousaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ousaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oncallusershiftannotation.Table, oncallusershiftannotation.FieldID, selector),
			sqlgraph.To(oncallusershift.Table, oncallusershift.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oncallusershiftannotation.ShiftTable, oncallusershiftannotation.ShiftColumn),
		)
		fromU = sqlgraph.SetNeighbors(ousaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OncallUserShiftAnnotation entity from the query.
// Returns a *NotFoundError when no OncallUserShiftAnnotation was found.
func (ousaq *OncallUserShiftAnnotationQuery) First(ctx context.Context) (*OncallUserShiftAnnotation, error) {
	nodes, err := ousaq.Limit(1).All(setContextOp(ctx, ousaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oncallusershiftannotation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) FirstX(ctx context.Context) *OncallUserShiftAnnotation {
	node, err := ousaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OncallUserShiftAnnotation ID from the query.
// Returns a *NotFoundError when no OncallUserShiftAnnotation ID was found.
func (ousaq *OncallUserShiftAnnotationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ousaq.Limit(1).IDs(setContextOp(ctx, ousaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oncallusershiftannotation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ousaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OncallUserShiftAnnotation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OncallUserShiftAnnotation entity is found.
// Returns a *NotFoundError when no OncallUserShiftAnnotation entities are found.
func (ousaq *OncallUserShiftAnnotationQuery) Only(ctx context.Context) (*OncallUserShiftAnnotation, error) {
	nodes, err := ousaq.Limit(2).All(setContextOp(ctx, ousaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oncallusershiftannotation.Label}
	default:
		return nil, &NotSingularError{oncallusershiftannotation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) OnlyX(ctx context.Context) *OncallUserShiftAnnotation {
	node, err := ousaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OncallUserShiftAnnotation ID in the query.
// Returns a *NotSingularError when more than one OncallUserShiftAnnotation ID is found.
// Returns a *NotFoundError when no entities are found.
func (ousaq *OncallUserShiftAnnotationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ousaq.Limit(2).IDs(setContextOp(ctx, ousaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oncallusershiftannotation.Label}
	default:
		err = &NotSingularError{oncallusershiftannotation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ousaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OncallUserShiftAnnotations.
func (ousaq *OncallUserShiftAnnotationQuery) All(ctx context.Context) ([]*OncallUserShiftAnnotation, error) {
	ctx = setContextOp(ctx, ousaq.ctx, ent.OpQueryAll)
	if err := ousaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OncallUserShiftAnnotation, *OncallUserShiftAnnotationQuery]()
	return withInterceptors[[]*OncallUserShiftAnnotation](ctx, ousaq, qr, ousaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) AllX(ctx context.Context) []*OncallUserShiftAnnotation {
	nodes, err := ousaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OncallUserShiftAnnotation IDs.
func (ousaq *OncallUserShiftAnnotationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ousaq.ctx.Unique == nil && ousaq.path != nil {
		ousaq.Unique(true)
	}
	ctx = setContextOp(ctx, ousaq.ctx, ent.OpQueryIDs)
	if err = ousaq.Select(oncallusershiftannotation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ousaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ousaq *OncallUserShiftAnnotationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ousaq.ctx, ent.OpQueryCount)
	if err := ousaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ousaq, querierCount[*OncallUserShiftAnnotationQuery](), ousaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) CountX(ctx context.Context) int {
	count, err := ousaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ousaq *OncallUserShiftAnnotationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ousaq.ctx, ent.OpQueryExist)
	switch _, err := ousaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ousaq *OncallUserShiftAnnotationQuery) ExistX(ctx context.Context) bool {
	exist, err := ousaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OncallUserShiftAnnotationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ousaq *OncallUserShiftAnnotationQuery) Clone() *OncallUserShiftAnnotationQuery {
	if ousaq == nil {
		return nil
	}
	return &OncallUserShiftAnnotationQuery{
		config:     ousaq.config,
		ctx:        ousaq.ctx.Clone(),
		order:      append([]oncallusershiftannotation.OrderOption{}, ousaq.order...),
		inters:     append([]Interceptor{}, ousaq.inters...),
		predicates: append([]predicate.OncallUserShiftAnnotation{}, ousaq.predicates...),
		withShift:  ousaq.withShift.Clone(),
		// clone intermediate query.
		sql:       ousaq.sql.Clone(),
		path:      ousaq.path,
		modifiers: append([]func(*sql.Selector){}, ousaq.modifiers...),
	}
}

// WithShift tells the query-builder to eager-load the nodes that are connected to
// the "shift" edge. The optional arguments are used to configure the query builder of the edge.
func (ousaq *OncallUserShiftAnnotationQuery) WithShift(opts ...func(*OncallUserShiftQuery)) *OncallUserShiftAnnotationQuery {
	query := (&OncallUserShiftClient{config: ousaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ousaq.withShift = query
	return ousaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ShiftID uuid.UUID `json:"shift_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OncallUserShiftAnnotation.Query().
//		GroupBy(oncallusershiftannotation.FieldShiftID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ousaq *OncallUserShiftAnnotationQuery) GroupBy(field string, fields ...string) *OncallUserShiftAnnotationGroupBy {
	ousaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OncallUserShiftAnnotationGroupBy{build: ousaq}
	grbuild.flds = &ousaq.ctx.Fields
	grbuild.label = oncallusershiftannotation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ShiftID uuid.UUID `json:"shift_id,omitempty"`
//	}
//
//	client.OncallUserShiftAnnotation.Query().
//		Select(oncallusershiftannotation.FieldShiftID).
//		Scan(ctx, &v)
func (ousaq *OncallUserShiftAnnotationQuery) Select(fields ...string) *OncallUserShiftAnnotationSelect {
	ousaq.ctx.Fields = append(ousaq.ctx.Fields, fields...)
	sbuild := &OncallUserShiftAnnotationSelect{OncallUserShiftAnnotationQuery: ousaq}
	sbuild.label = oncallusershiftannotation.Label
	sbuild.flds, sbuild.scan = &ousaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OncallUserShiftAnnotationSelect configured with the given aggregations.
func (ousaq *OncallUserShiftAnnotationQuery) Aggregate(fns ...AggregateFunc) *OncallUserShiftAnnotationSelect {
	return ousaq.Select().Aggregate(fns...)
}

func (ousaq *OncallUserShiftAnnotationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ousaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ousaq); err != nil {
				return err
			}
		}
	}
	for _, f := range ousaq.ctx.Fields {
		if !oncallusershiftannotation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ousaq.path != nil {
		prev, err := ousaq.path(ctx)
		if err != nil {
			return err
		}
		ousaq.sql = prev
	}
	return nil
}

func (ousaq *OncallUserShiftAnnotationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OncallUserShiftAnnotation, error) {
	var (
		nodes       = []*OncallUserShiftAnnotation{}
		_spec       = ousaq.querySpec()
		loadedTypes = [1]bool{
			ousaq.withShift != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OncallUserShiftAnnotation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OncallUserShiftAnnotation{config: ousaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ousaq.modifiers) > 0 {
		_spec.Modifiers = ousaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ousaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ousaq.withShift; query != nil {
		if err := ousaq.loadShift(ctx, query, nodes, nil,
			func(n *OncallUserShiftAnnotation, e *OncallUserShift) { n.Edges.Shift = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ousaq *OncallUserShiftAnnotationQuery) loadShift(ctx context.Context, query *OncallUserShiftQuery, nodes []*OncallUserShiftAnnotation, init func(*OncallUserShiftAnnotation), assign func(*OncallUserShiftAnnotation, *OncallUserShift)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*OncallUserShiftAnnotation)
	for i := range nodes {
		fk := nodes[i].ShiftID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(oncallusershift.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shift_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ousaq *OncallUserShiftAnnotationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ousaq.querySpec()
	if len(ousaq.modifiers) > 0 {
		_spec.Modifiers = ousaq.modifiers
	}
	_spec.Node.Columns = ousaq.ctx.Fields
	if len(ousaq.ctx.Fields) > 0 {
		_spec.Unique = ousaq.ctx.Unique != nil && *ousaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ousaq.driver, _spec)
}

func (ousaq *OncallUserShiftAnnotationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oncallusershiftannotation.Table, oncallusershiftannotation.Columns, sqlgraph.NewFieldSpec(oncallusershiftannotation.FieldID, field.TypeUUID))
	_spec.From = ousaq.sql
	if unique := ousaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ousaq.path != nil {
		_spec.Unique = true
	}
	if fields := ousaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oncallusershiftannotation.FieldID)
		for i := range fields {
			if fields[i] != oncallusershiftannotation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ousaq.withShift != nil {
			_spec.Node.AddColumnOnce(oncallusershiftannotation.FieldShiftID)
		}
	}
	if ps := ousaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ousaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ousaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ousaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ousaq *OncallUserShiftAnnotationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ousaq.driver.Dialect())
	t1 := builder.Table(oncallusershiftannotation.Table)
	columns := ousaq.ctx.Fields
	if len(columns) == 0 {
		columns = oncallusershiftannotation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ousaq.sql != nil {
		selector = ousaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ousaq.ctx.Unique != nil && *ousaq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ousaq.modifiers {
		m(selector)
	}
	for _, p := range ousaq.predicates {
		p(selector)
	}
	for _, p := range ousaq.order {
		p(selector)
	}
	if offset := ousaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ousaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ousaq *OncallUserShiftAnnotationQuery) Modify(modifiers ...func(s *sql.Selector)) *OncallUserShiftAnnotationSelect {
	ousaq.modifiers = append(ousaq.modifiers, modifiers...)
	return ousaq.Select()
}

// OncallUserShiftAnnotationGroupBy is the group-by builder for OncallUserShiftAnnotation entities.
type OncallUserShiftAnnotationGroupBy struct {
	selector
	build *OncallUserShiftAnnotationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ousagb *OncallUserShiftAnnotationGroupBy) Aggregate(fns ...AggregateFunc) *OncallUserShiftAnnotationGroupBy {
	ousagb.fns = append(ousagb.fns, fns...)
	return ousagb
}

// Scan applies the selector query and scans the result into the given value.
func (ousagb *OncallUserShiftAnnotationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ousagb.build.ctx, ent.OpQueryGroupBy)
	if err := ousagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OncallUserShiftAnnotationQuery, *OncallUserShiftAnnotationGroupBy](ctx, ousagb.build, ousagb, ousagb.build.inters, v)
}

func (ousagb *OncallUserShiftAnnotationGroupBy) sqlScan(ctx context.Context, root *OncallUserShiftAnnotationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ousagb.fns))
	for _, fn := range ousagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ousagb.flds)+len(ousagb.fns))
		for _, f := range *ousagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ousagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ousagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OncallUserShiftAnnotationSelect is the builder for selecting fields of OncallUserShiftAnnotation entities.
type OncallUserShiftAnnotationSelect struct {
	*OncallUserShiftAnnotationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ousas *OncallUserShiftAnnotationSelect) Aggregate(fns ...AggregateFunc) *OncallUserShiftAnnotationSelect {
	ousas.fns = append(ousas.fns, fns...)
	return ousas
}

// Scan applies the selector query and scans the result into the given value.
func (ousas *OncallUserShiftAnnotationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ousas.ctx, ent.OpQuerySelect)
	if err := ousas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OncallUserShiftAnnotationQuery, *OncallUserShiftAnnotationSelect](ctx, ousas.OncallUserShiftAnnotationQuery, ousas, ousas.inters, v)
}

func (ousas *OncallUserShiftAnnotationSelect) sqlScan(ctx context.Context, root *OncallUserShiftAnnotationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ousas.fns))
	for _, fn := range ousas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ousas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ousas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ousas *OncallUserShiftAnnotationSelect) Modify(modifiers ...func(s *sql.Selector)) *OncallUserShiftAnnotationSelect {
	ousas.modifiers = append(ousas.modifiers, modifiers...)
	return ousas
}
