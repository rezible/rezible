// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/rezible/rezible/ent/oncallalert"
	"github.com/rezible/rezible/ent/oncallroster"
	"github.com/rezible/rezible/ent/predicate"
)

// OncallAlertQuery is the builder for querying OncallAlert entities.
type OncallAlertQuery struct {
	config
	ctx        *QueryContext
	order      []oncallalert.OrderOption
	inters     []Interceptor
	predicates []predicate.OncallAlert
	withRoster *OncallRosterQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OncallAlertQuery builder.
func (oaq *OncallAlertQuery) Where(ps ...predicate.OncallAlert) *OncallAlertQuery {
	oaq.predicates = append(oaq.predicates, ps...)
	return oaq
}

// Limit the number of records to be returned by this query.
func (oaq *OncallAlertQuery) Limit(limit int) *OncallAlertQuery {
	oaq.ctx.Limit = &limit
	return oaq
}

// Offset to start from.
func (oaq *OncallAlertQuery) Offset(offset int) *OncallAlertQuery {
	oaq.ctx.Offset = &offset
	return oaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oaq *OncallAlertQuery) Unique(unique bool) *OncallAlertQuery {
	oaq.ctx.Unique = &unique
	return oaq
}

// Order specifies how the records should be ordered.
func (oaq *OncallAlertQuery) Order(o ...oncallalert.OrderOption) *OncallAlertQuery {
	oaq.order = append(oaq.order, o...)
	return oaq
}

// QueryRoster chains the current query on the "roster" edge.
func (oaq *OncallAlertQuery) QueryRoster() *OncallRosterQuery {
	query := (&OncallRosterClient{config: oaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := oaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := oaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oncallalert.Table, oncallalert.FieldID, selector),
			sqlgraph.To(oncallroster.Table, oncallroster.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, oncallalert.RosterTable, oncallalert.RosterColumn),
		)
		fromU = sqlgraph.SetNeighbors(oaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OncallAlert entity from the query.
// Returns a *NotFoundError when no OncallAlert was found.
func (oaq *OncallAlertQuery) First(ctx context.Context) (*OncallAlert, error) {
	nodes, err := oaq.Limit(1).All(setContextOp(ctx, oaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oncallalert.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oaq *OncallAlertQuery) FirstX(ctx context.Context) *OncallAlert {
	node, err := oaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OncallAlert ID from the query.
// Returns a *NotFoundError when no OncallAlert ID was found.
func (oaq *OncallAlertQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = oaq.Limit(1).IDs(setContextOp(ctx, oaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oncallalert.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oaq *OncallAlertQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := oaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OncallAlert entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OncallAlert entity is found.
// Returns a *NotFoundError when no OncallAlert entities are found.
func (oaq *OncallAlertQuery) Only(ctx context.Context) (*OncallAlert, error) {
	nodes, err := oaq.Limit(2).All(setContextOp(ctx, oaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oncallalert.Label}
	default:
		return nil, &NotSingularError{oncallalert.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oaq *OncallAlertQuery) OnlyX(ctx context.Context) *OncallAlert {
	node, err := oaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OncallAlert ID in the query.
// Returns a *NotSingularError when more than one OncallAlert ID is found.
// Returns a *NotFoundError when no entities are found.
func (oaq *OncallAlertQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = oaq.Limit(2).IDs(setContextOp(ctx, oaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oncallalert.Label}
	default:
		err = &NotSingularError{oncallalert.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oaq *OncallAlertQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := oaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OncallAlerts.
func (oaq *OncallAlertQuery) All(ctx context.Context) ([]*OncallAlert, error) {
	ctx = setContextOp(ctx, oaq.ctx, ent.OpQueryAll)
	if err := oaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OncallAlert, *OncallAlertQuery]()
	return withInterceptors[[]*OncallAlert](ctx, oaq, qr, oaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (oaq *OncallAlertQuery) AllX(ctx context.Context) []*OncallAlert {
	nodes, err := oaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OncallAlert IDs.
func (oaq *OncallAlertQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if oaq.ctx.Unique == nil && oaq.path != nil {
		oaq.Unique(true)
	}
	ctx = setContextOp(ctx, oaq.ctx, ent.OpQueryIDs)
	if err = oaq.Select(oncallalert.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oaq *OncallAlertQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := oaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oaq *OncallAlertQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, oaq.ctx, ent.OpQueryCount)
	if err := oaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, oaq, querierCount[*OncallAlertQuery](), oaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (oaq *OncallAlertQuery) CountX(ctx context.Context) int {
	count, err := oaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oaq *OncallAlertQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, oaq.ctx, ent.OpQueryExist)
	switch _, err := oaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (oaq *OncallAlertQuery) ExistX(ctx context.Context) bool {
	exist, err := oaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OncallAlertQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oaq *OncallAlertQuery) Clone() *OncallAlertQuery {
	if oaq == nil {
		return nil
	}
	return &OncallAlertQuery{
		config:     oaq.config,
		ctx:        oaq.ctx.Clone(),
		order:      append([]oncallalert.OrderOption{}, oaq.order...),
		inters:     append([]Interceptor{}, oaq.inters...),
		predicates: append([]predicate.OncallAlert{}, oaq.predicates...),
		withRoster: oaq.withRoster.Clone(),
		// clone intermediate query.
		sql:       oaq.sql.Clone(),
		path:      oaq.path,
		modifiers: append([]func(*sql.Selector){}, oaq.modifiers...),
	}
}

// WithRoster tells the query-builder to eager-load the nodes that are connected to
// the "roster" edge. The optional arguments are used to configure the query builder of the edge.
func (oaq *OncallAlertQuery) WithRoster(opts ...func(*OncallRosterQuery)) *OncallAlertQuery {
	query := (&OncallRosterClient{config: oaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	oaq.withRoster = query
	return oaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RosterID uuid.UUID `json:"roster_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OncallAlert.Query().
//		GroupBy(oncallalert.FieldRosterID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (oaq *OncallAlertQuery) GroupBy(field string, fields ...string) *OncallAlertGroupBy {
	oaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OncallAlertGroupBy{build: oaq}
	grbuild.flds = &oaq.ctx.Fields
	grbuild.label = oncallalert.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RosterID uuid.UUID `json:"roster_id,omitempty"`
//	}
//
//	client.OncallAlert.Query().
//		Select(oncallalert.FieldRosterID).
//		Scan(ctx, &v)
func (oaq *OncallAlertQuery) Select(fields ...string) *OncallAlertSelect {
	oaq.ctx.Fields = append(oaq.ctx.Fields, fields...)
	sbuild := &OncallAlertSelect{OncallAlertQuery: oaq}
	sbuild.label = oncallalert.Label
	sbuild.flds, sbuild.scan = &oaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OncallAlertSelect configured with the given aggregations.
func (oaq *OncallAlertQuery) Aggregate(fns ...AggregateFunc) *OncallAlertSelect {
	return oaq.Select().Aggregate(fns...)
}

func (oaq *OncallAlertQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range oaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, oaq); err != nil {
				return err
			}
		}
	}
	for _, f := range oaq.ctx.Fields {
		if !oncallalert.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if oaq.path != nil {
		prev, err := oaq.path(ctx)
		if err != nil {
			return err
		}
		oaq.sql = prev
	}
	return nil
}

func (oaq *OncallAlertQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OncallAlert, error) {
	var (
		nodes       = []*OncallAlert{}
		_spec       = oaq.querySpec()
		loadedTypes = [1]bool{
			oaq.withRoster != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OncallAlert).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OncallAlert{config: oaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(oaq.modifiers) > 0 {
		_spec.Modifiers = oaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := oaq.withRoster; query != nil {
		if err := oaq.loadRoster(ctx, query, nodes, nil,
			func(n *OncallAlert, e *OncallRoster) { n.Edges.Roster = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (oaq *OncallAlertQuery) loadRoster(ctx context.Context, query *OncallRosterQuery, nodes []*OncallAlert, init func(*OncallAlert), assign func(*OncallAlert, *OncallRoster)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*OncallAlert)
	for i := range nodes {
		fk := nodes[i].RosterID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(oncallroster.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "roster_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (oaq *OncallAlertQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oaq.querySpec()
	if len(oaq.modifiers) > 0 {
		_spec.Modifiers = oaq.modifiers
	}
	_spec.Node.Columns = oaq.ctx.Fields
	if len(oaq.ctx.Fields) > 0 {
		_spec.Unique = oaq.ctx.Unique != nil && *oaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, oaq.driver, _spec)
}

func (oaq *OncallAlertQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oncallalert.Table, oncallalert.Columns, sqlgraph.NewFieldSpec(oncallalert.FieldID, field.TypeUUID))
	_spec.From = oaq.sql
	if unique := oaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if oaq.path != nil {
		_spec.Unique = true
	}
	if fields := oaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oncallalert.FieldID)
		for i := range fields {
			if fields[i] != oncallalert.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if oaq.withRoster != nil {
			_spec.Node.AddColumnOnce(oncallalert.FieldRosterID)
		}
	}
	if ps := oaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oaq *OncallAlertQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oaq.driver.Dialect())
	t1 := builder.Table(oncallalert.Table)
	columns := oaq.ctx.Fields
	if len(columns) == 0 {
		columns = oncallalert.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oaq.sql != nil {
		selector = oaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oaq.ctx.Unique != nil && *oaq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range oaq.modifiers {
		m(selector)
	}
	for _, p := range oaq.predicates {
		p(selector)
	}
	for _, p := range oaq.order {
		p(selector)
	}
	if offset := oaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (oaq *OncallAlertQuery) Modify(modifiers ...func(s *sql.Selector)) *OncallAlertSelect {
	oaq.modifiers = append(oaq.modifiers, modifiers...)
	return oaq.Select()
}

// OncallAlertGroupBy is the group-by builder for OncallAlert entities.
type OncallAlertGroupBy struct {
	selector
	build *OncallAlertQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (oagb *OncallAlertGroupBy) Aggregate(fns ...AggregateFunc) *OncallAlertGroupBy {
	oagb.fns = append(oagb.fns, fns...)
	return oagb
}

// Scan applies the selector query and scans the result into the given value.
func (oagb *OncallAlertGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oagb.build.ctx, ent.OpQueryGroupBy)
	if err := oagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OncallAlertQuery, *OncallAlertGroupBy](ctx, oagb.build, oagb, oagb.build.inters, v)
}

func (oagb *OncallAlertGroupBy) sqlScan(ctx context.Context, root *OncallAlertQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(oagb.fns))
	for _, fn := range oagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*oagb.flds)+len(oagb.fns))
		for _, f := range *oagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*oagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OncallAlertSelect is the builder for selecting fields of OncallAlert entities.
type OncallAlertSelect struct {
	*OncallAlertQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (oas *OncallAlertSelect) Aggregate(fns ...AggregateFunc) *OncallAlertSelect {
	oas.fns = append(oas.fns, fns...)
	return oas
}

// Scan applies the selector query and scans the result into the given value.
func (oas *OncallAlertSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oas.ctx, ent.OpQuerySelect)
	if err := oas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OncallAlertQuery, *OncallAlertSelect](ctx, oas.OncallAlertQuery, oas, oas.inters, v)
}

func (oas *OncallAlertSelect) sqlScan(ctx context.Context, root *OncallAlertQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(oas.fns))
	for _, fn := range oas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*oas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (oas *OncallAlertSelect) Modify(modifiers ...func(s *sql.Selector)) *OncallAlertSelect {
	oas.modifiers = append(oas.modifiers, modifiers...)
	return oas
}
