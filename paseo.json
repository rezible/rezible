{
  "worktree": {
    "setup":    [
      "cp \"$PASEO_SOURCE_CHECKOUT_PATH/.env\" .env",
      "just dev setup"
    ]
  },
  "scripts": {
    "test-backend": { "command": "just test backend" },
    "dev": { "command": "HTTP__AUTH__ENABLE_DEV_SKIP_MODE=true just dev" },
    "codegen": { "command": "just codegen" }
  }
}
